function Task({title, body}) {
    // components which holds tasks
    
    return (
        <div className="">
            <h3>{title}</h3>
            <p>{body}</p>
        </div>
    );
}

export default Task;